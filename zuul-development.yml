zuul:
  #ignored-patterns: /inventory-api/** 
  routes:
    inventory-service:
      path: /inventory-api/**
      service-id: inventory-service 
    product-service:
      path: /product-api/**
      service-id: product-service
management:
  security:
    enabled=false
  endpoints:
    web:
       exposure:
         include=*
